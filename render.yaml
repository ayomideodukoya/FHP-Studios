services:
  - type: web
    name: fhp-backend
    env: python
    buildCommand: "cd backend && pip install -r requirements.txt"
    startCommand: "cd backend && ./start.sh"
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: PORT
        value: 10000
