# Enable only after migration, credentials and end-to-end checks succeed.
BOOKING_ENABLED=false
SITE_URL=https://your-domain.example
SUPABASE_URL=https://dzqzyocmunhmavjjqtzy.supabase.co
# Server only: never prefix this key with NEXT_PUBLIC_.
SUPABASE_SECRET_KEY=
# A random server secret used to hash rate-limit identifiers.
BOOKING_RATE_SECRET=
# International contact number, with country code. Set in Vercel, not Git.
# Redeploy after changing. The resulting WhatsApp link is public to visitors.
FHP_WHATSAPP_NUMBER=
